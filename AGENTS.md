# AGENTS.md

Spec Kit workspace context is installed at the workspace root.

For tasks in this repository, first read `../AGENTS.md` and follow the
workspace-level `.specify`, `ai`, and `.agents` assets there. This repository
keeps this file only as a compatibility pointer for agents launched from the
Spec Kit source repository.
